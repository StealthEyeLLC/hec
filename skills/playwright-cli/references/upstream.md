# Upstream provenance

This HEC Skill is adapted from the official Playwright CLI Agent Skill bundled with:

```text
@playwright/cli 0.1.17
```

Upstream project:

```text
microsoft/playwright-cli
```

The upstream package and copied/adapted command guidance are licensed under Apache License 2.0. Preserve upstream notices when redistributing copied upstream material. The HEC-specific session layout, artifact workflow, directory conventions, and operation-selection guidance are adaptations for the HEC workstation.
